TIPOS PRIMITIVOS
bool 
int
string
float64

//DECLARACIONES DE VARIABLES
mut variable Tipo = valor
mut variable := valor
mut variable Tipo   // VALOR POR DEFECTO
variable := valor

EJEMPLOS
mut var1 int = 200
mut var2 := 120
mut var3 int   // VALOR POR DEFECTO
var4 := 100

REASIGNACION
variable  = valor

INCREMENTO/DECREMENTO
variable  += valor
variable  -= valor

IMPRIMIR
print()  
println()
print( a, a , a)  //Impresiones Multiples

CICLOS
if else 

FUNCIONES
fn nombre(){}
fn nombre()tipo {}
fn nombre(id tipo, id tipo) {}
fn nombre(id tipo, id tipo) int{}

OPERACIONES ARIMETICAS
+ - / * %
OPERACIONES RELACIONALES
== != < >  <= >=
OPERACIONES LOGICAS
&& || !

CONTROL DE TRASNFERENCIA
return 
continue 
break

FUNCIONES EMBEBIDAS
atoi
parserFloat
typeOf

SLICES
numbers = []int {1, 2, 3, 4, 5} // con datos
numbers = []int {} vacio
append()
len()
IndexOf()
join()
numeros[2] = 100   //Reasignar valor
print(numeros[2])        // Acceso Elemento
print(numeros)    //imprimir todo el vector

SLICES MULTIPLES 
mtx2 = [][]int{
    {1, 2, 3},
    {7, 8, 9},
    {4, 5, 6}   // QUITAR LA COMA DEL ULTIMO {}
}
mtx2[0][0] = 7 //Reasignar valor
print(mtx2)    //imprimir toda la Matriz 
print(mtx2[0][1]) // Imprime el Valor en esa Posicion

numeros = []int{10, 11, 12}  // Vector 
mtx2 = append(mtx2, numeros)    //Agregar un Vector  a la Matriz
print(len(mtx2)) // Cuantas Filas tiene la Matriz

buscar := indexOf(mtx2, 7)                     
print("Posición del 7:", buscar) //Buscar un Elemento de la Matriz

STRUCTS

struct Persona {
    string Nombre
    int Edad  
    bool EsEstudiante
}

// Creación de una instancia 
miInstancia := Persona{
    Nombre: "Alice",
    Edad: 25,
    EsEstudiante: false  
}

// Acceso a campos 
print("Nombre:", miInstancia.Nombre)        
print("Edad:", miInstancia.Edad)              
print("Estudiante:", miInstancia.EsEstudiante) 

// Modificación 
miInstancia.Nombre = "Bob"
miInstancia.Edad = 30
print("Nuevo nombre:", miInstancia.Nombre) 
print("Nueva Edad:", miInstancia.Edad)  

STRUCTS CON FUNCIONES
//AUN NO FUNCIONAN
struct Persona {
    string Nombre
    int Edad  
}

persona1 := Persona{
     Nombre: "Ana",
     Edad: 30
     }

// Método Saludar (no modifica atributos)
func (p *Persona) Saludar() string {
    return "Hola, soy " + p.Nombre
}

mensaje := persona1.Saludar()
    print(mensaje)  // "Hola, soy Ana"